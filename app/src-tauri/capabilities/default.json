{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "The one window: engine calls go through the app's own commands; the dialog plugin opens deck.json.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-webview-position",
    "dialog:allow-open"
  ]
}
