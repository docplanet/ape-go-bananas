{
  "name": "ape-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The A.P.E. download page, and the browser tool: checks, review and .apkg export with nothing installed.",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pretest": "npm --prefix .. run build",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "sql.js": "^1.13.0"
  },
  "devDependencies": {
    "@types/sql.js": "^1.4.9",
    "typescript": "6.0.3",
    "vite": "8.2.2"
  }
}
